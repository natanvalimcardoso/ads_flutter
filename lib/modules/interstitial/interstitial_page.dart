import 'package:flutter/material.dart';

class InterstitialPage extends StatelessWidget {
  const InterstitialPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('IntertitialPage'),
      ),
      body: Container(),
    );
  }
}
